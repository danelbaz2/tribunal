# Slide 55 — the local context file never enters git
CLAUDE.local.md

# secrets
.env
.env.*
# ...but the template that says which two they are is committed
!.env.example

# lesson material and scratch extractions
pptx.txt
*.pptx

# python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/

# node
node_modules/
dist/

# sqlite used by the offline tests and smoke runs
*.sqlite
